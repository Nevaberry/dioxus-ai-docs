# Agent Development Kit (ADK)

Open-source framework from Google for building multi-agent AI systems. Supports Python, TypeScript, Go, Java.

## Installation

```bash
pip install google-adk     # Python
npm install @google/adk    # TypeScript
```

## Cloud Run Integration

Cloud Run has built-in ADK entrypoint detection for Python source deployments.
