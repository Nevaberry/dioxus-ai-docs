# Go Tooling (1.26)

## `go fix` Modernizers

`go fix` has been revamped with dozens of modernizer analyzers that update code to use newer language/stdlib idioms. Supports `//go:fix inline` directive for custom API migration.
